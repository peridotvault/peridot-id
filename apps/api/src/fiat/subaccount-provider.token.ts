import type { SubAccountProvider } from "@peridotvault/pid-payments";

/** Injection token for the Sub-Account (V2) provider. Backend-only. */
export const SUBACCOUNT_PROVIDER = "SUBACCOUNT_PROVIDER";
export type { SubAccountProvider };
