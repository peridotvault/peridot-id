import type { DokuCheckoutClient } from "@peridotvault/pid-payments";

/** Injection token for the DOKU Checkout (money-in) client. Backend-only. */
export const CHECKOUT_CLIENT = "CHECKOUT_CLIENT";
export type { DokuCheckoutClient };
