# Copy to .env.local and adjust for local dev.
# Web dev server runs on :3300, API on :3301.
NEXT_PUBLIC_PID_API_URL=http://localhost:3301
# Wallet URL for the workspace "Go to wallet" link (prod default is https://app.pid.peridotvault.com).
NEXT_PUBLIC_PID_WALLET_URL=http://localhost:8081
