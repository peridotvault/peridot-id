{
  "name": "@peridotvault/pid-payments",
  "version": "0.1.0",
  "description": "PeridotID fiat gateway primitives — DOKU Checkout (hosted page) signature helpers and shared fiat types. No chain imports.",
  "license": "MIT",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "prepack": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "@types/node": "^22.20.1"
  }
}
